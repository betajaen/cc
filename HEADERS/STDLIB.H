extern int*  calloc(nelem, elsize);                          /* int nelem, int elsize */
extern int   cfree(ptr);                                     /* int* ptr */
extern int   exit(status);                                   /* int status */
extern int   zero(ptr, memsize);                             /* int* ptr, int memsize */

#define TRUE    1
#define FALSE   0
